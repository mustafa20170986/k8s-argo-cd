apiVersion: apps/v1
kind: Deployment
metadata:
  name: adduser
  namespace: suborna
spec:
  replicas: 2
  selector:
    matchLabels:
      app: adduser-express
  template:
    metadata:
      labels:
        app: adduser-express
    spec:
      containers:
        - name: adduser-container
          image: adduser
          imagePullPolicy: Never
          resources:
            requests:
              memory: "100Mi"
              cpu: "50m"
            limits:
              memory: "200Mi"
              cpu: "100m"
          env:
            - name: DATABASE_URL
              valueFrom: 
                secretKeyRef:
                  name: sbrn-sec
                  key: DATABASE_URL
          ports:
            - containerPort: 4045